import{AdminTable}from"@/components/admin/admin-table";import{requireRole}from"@/lib/portal-auth";import{serviceSelect}from"@/lib/supabase-rpc";import{money}from"@/lib/portal-format";export default async function Subscriptions(){await requireRole(['administrator']);const rows=await serviceSelect<Array<{status:string;frequency:string;billing_interval:string;billed_price_cents:number;currency:string;customers:{full_name:string}}>>('subscriptions?select=status,frequency,billing_interval,billed_price_cents,currency,customers(full_name)&order=created_at.desc&limit=500');return <AdminTable title="Subscriptions" headers={['Customer','Status','Frequency','Billing','Amount']} rows={rows.map(r=>[r.customers.full_name,r.status,r.frequency,r.billing_interval,money(r.billed_price_cents,r.currency)])}/>}
