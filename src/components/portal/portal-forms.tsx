"use client";
import * as React from "react";import type { PortalCopy } from "@/i18n/portal-copy";import {buttonVariants} from "@/components/ui/button";import {cn} from "@/lib/utils";
export function ProposalAction({id,label}:{id:string;label:string}){const[busy,setBusy]=React.useState(false);async function accept(){setBusy(true);const r=await fetch(`/api/account/proposals/${id}/checkout`,{method:"POST"});const data=await r.json().catch(()=>({})) as {checkoutUrl?:string};if(data.checkoutUrl)location.assign(data.checkoutUrl);else setBusy(false)}return <button disabled={busy} onClick={accept} className={buttonVariants({variant:"primary",size:"md"})}>{label}</button>}
export function ProfileForm({copy,initial}:{copy:PortalCopy["dashboard"];initial:{first_name:string|null;last_name:string|null;phone:string;whatsapp_number:string|null;preferred_language:string}}){const[saved,setSaved]=React.useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/account/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});setSaved(r.ok)}return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-2"><input className="input" name="firstName" defaultValue={initial.first_name||""} autoComplete="given-name"/><input className="input" name="lastName" defaultValue={initial.last_name||""} autoComplete="family-name"/><input className="input" name="phone" defaultValue={initial.phone} autoComplete="tel"/><input className="input" name="whatsapp" defaultValue={initial.whatsapp_number||""} autoComplete="tel"/><button className={cn(buttonVariants({variant:"primary",size:"md"}),"sm:col-span-2")}>{saved?copy.saved:copy.save}</button></form>}
export function SupportForm({copy}:{copy:PortalCopy["dashboard"]}){const[saved,setSaved]=React.useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));const r=await fetch("/api/account/support",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(r.ok){setSaved(true);form.reset()}}return <form onSubmit={submit} className="space-y-4"><label className="block text-sm">{copy.subject}<input name="subject" required maxLength={160} className="input mt-2"/></label><label className="block text-sm">{copy.message}<textarea name="message" required maxLength={4000} rows={6} className="input mt-2 h-auto py-3"/></label><button className={buttonVariants({variant:"primary",size:"md"})}>{saved?copy.saved:copy.contactSupport}</button></form>}
