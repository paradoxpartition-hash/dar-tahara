import type{ReactNode}from"react";export function AdminTable({title,headers,rows}:{title:string;headers:string[];rows:ReactNode[][]}){return <div><h1 className="font-serif text-4xl">{title}</h1><div className="mt-7 overflow-x-auto rounded-2xl border border-border bg-card"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-secondary/60 text-xs uppercase tracking-wider text-muted-foreground"><tr>{headers.map(h=><th key={h} className="p-4">{h}</th>)}</tr></thead><tbody className="divide-y divide-border">{rows.map((row,i)=><tr key={i}>{row.map((cell,j)=><td key={j} className="p-4">{cell}</td>)}</tr>)}{!rows.length?<tr><td colSpan={headers.length} className="p-10 text-center text-muted-foreground">No records.</td></tr>:null}</tbody></table></div></div>}
